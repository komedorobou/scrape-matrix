# TODAYFUL スクレイピング操作ログ（2026/3/28）

## 環境
- Mac: ~/Desktop/個人/scrape-matrix/
- Python: venv/bin/python3.14
- Streamlit: localhost:8501

## 操作手順（実際に実行した内容）

### 1. 起動
```
open ~/Desktop/個人/scrape-matrix/起動.command
```
→ ターミナルが開き、Streamlitが http://localhost:8501 で起動
→ ブラウザが自動で開く

### 2. Seleniumで自動操作

#### 2-1. コメ兵とRAGTAGを削除
```python
for target in ['コメ兵', 'RAGTAG']:
    tags = driver.find_elements(By.CSS_SELECTOR, '[data-baseweb="tag"]')
    for tag in tags:
        if target in tag.text:
            btn = tag.find_element(By.CSS_SELECTOR, '[role="presentation"]')
            btn.click()
            time.sleep(1)
            break
```
※1個削除するとDOMが更新されるので、毎回 `find_elements` し直す

#### 2-2. ブランド名入力
```python
brand_input = driver.find_element(By.CSS_SELECTOR, 'input[aria-label="ブランド名（英語）"]')
brand_input.click()
brand_input.send_keys(Keys.COMMAND + 'a')
brand_input.send_keys('TODAYFUL')
brand_input.send_keys(Keys.TAB)
```

#### 2-3. 全件取得にチェック
```python
containers = driver.find_elements(By.CSS_SELECTOR, '[data-testid="stCheckbox"]')
for c in containers:
    if '全件取得' in c.text:
        c.click()
        break
```

#### 2-4. スクレイピング開始
```python
start_btn = driver.find_element(By.XPATH, '//button[contains(., "スクレイピング開始")]')
driver.execute_script("arguments[0].scrollIntoView(true);", start_btn)
start_btn.click()
```

#### 2-5. 完了待ち（監視ループ）
```python
for i in range(360):
    time.sleep(10)
    text = driver.find_element(By.TAG_NAME, 'body').text
    if 'ダウンロード' in text:
        print("完了！")
        break
```

### 3. スクレイピング結果
- トレファク: 723件
- セカスト: 1,598件
- 合計: 2,321件
- ユニーク品番: 797件

### 4. Excelダウンロード（1回目：全データ）
```python
dl_btn = driver.find_element(By.XPATH, '//button[contains(., "Excelダウンロード")]')
driver.execute_script("arguments[0].click();", dl_btn)
```
→ ~/Downloads/allsites_TODAYFUL_20260328_154222.xlsx (398KB)

### 5. Web検索で補完にチェック
```python
checkboxes = driver.find_elements(By.CSS_SELECTOR, '[data-testid="stCheckbox"]')
for c in checkboxes:
    if 'Web検索で補完' in c.text:
        c.click()
        break
```

### 6. 正式名称を取得
```python
name_btn = driver.find_element(By.XPATH, '//button[contains(., "正式名称を取得")]')
driver.execute_script("arguments[0].scrollIntoView(true);", name_btn)
driver.execute_script("arguments[0].click();", name_btn)
```
→ WEAR検索で品番→正式名称を補完
→ KDE分析: 528件を除外（代表価格≤¥10,000）
→ 名称未特定: 103件

### 7. Excelダウンロード（2回目：分析結果）
```python
# 「正式名称を取得」ボタンより下のダウンロードボタンを探す
# 除外品番メッセージが出たら完了
buttons = driver.find_elements(By.TAG_NAME, 'button')
dl_btns = [b for b in buttons if 'ダウンロード' in b.text]
last_dl = dl_btns[-1]  # 一番下のやつ
driver.execute_script("arguments[0].click();", last_dl)
```
→ ~/Downloads/TODAYFUL_analysis_20260328_154825.xlsx (16KB)

## 注意点
- Seleniumのクリックが被る場合は `driver.execute_script("arguments[0].click();", btn)` でJS経由
- タグ削除は1個ずつ（StaleElementReference対策）
- セカストはAkamaiボット検知あり → app.pyに自動突破コード追加済み（2026/3/28）
- スリープ防止: `caffeinate -d` を実行しておく
- 起動.commandのpipパスはvenv内を直接指定に修正済み
